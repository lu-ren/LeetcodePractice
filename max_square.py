def max_square(m):
    height, width = len(m), len(m[0])

    # Scanning left to right, up to down for max squares
    for i in range(height):
        for j in range(width):
            pass

def get_square_area(m, i j):
    pass
